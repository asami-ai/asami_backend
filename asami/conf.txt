# This is the configuration file for your local development environment. By
# default, it runs a single server on port 8080. To set up your server, you will
# need to add the name of the database you are connecting to and the user name.
# The password for the database is stored in the config/passwords.yaml.
#
# When running your server locally, the server ports are the same as the public
# facing ports.

# Configuration for the main API server.
apiServer:
  port: 8080
  publicHost: localhost
  publicPort: 8080
  publicScheme: http

# Configuration for the Insights server.
insightsServer:
  port: 8081
  publicHost: localhost
  publicPort: 8081
  publicScheme: http

# Configuration for the web server.
webServer:
  port: 8082
  publicHost: localhost
  publicPort: 8082
  publicScheme: http

# This is the database setup for your server.
database:
  host: localhost
  port: 8090
  name: asami
  user: postgres
  #requireSsl: true # defaults to false
  #isUnixSocket: true # defaults to false

# This is the setup for Redis.
redis:
  enabled: false
  host: localhost
  port: 8091
  #user: # defaults to empty
  #requireSsl: true # defaults to false

maxRequestSize: 524288 # The maximum size of requests allowed in bytes

sessionLogs:
  persistentEnabled: true
  consoleEnabled: true
  consoleLogFormat: text # Defaults to "json", options are "text" or "json"

# futureCallExecutionEnabled: true # Defaults to true

#futureCall:
#  concurrencyLimit: 1 # Defaults to 1, a negative or null value removes the limit
#  scanInterval: 5000 # Unit in milliseconds, defaults to 5000





alright, I'm at the second stage of the asami project. the spec file explains what I'm curretly working on. if you check, you will see that the authservice, authendpoint, even new yaml model files have been created and some others updated.  now as much as I have mplemented this, on the botendpoint, when a users message is being processed and the auth check being carried out(I stand to be corrected), the implementation i stated was supposed to work this way, and though, i have written the files, they are not connecting:

Auth check, checks if there is user with that platform id, and if no, notifies user that it seems it is its first time and gives the option to either register or login, but if yes, proceeds to check if the email is verifies, if no, then it probably means that the user have been registered, but the users mail is yet to be verified, and proceeds to ask the user to input the code sent, or if expired asks the user to request for another one. but if the user is already verified via email, continues the regular message processing. now lets imagine the option of login in is choosing by the user, then it is assumed that the user is a returning user, but maybe registered on (lets say the new platform he/she is chatting on is whatsapp, but registered on telegram), so the user is asked to input the email, and the check is made to know if a user with that email exists, (now this check ignores the users platformId, but uses the email supplied to hit the db and get a user) if the user exists, a code is sent to the email, and the user is asked to verify email, by sending back the code. and if correct, the user is allowed to continue messaging the bot. now what happens during this process of verification, is the users account created for each platform, no. the yaml model file and the spec files explains. if a user, with email exists and the email is correctly verified, then(if telegram is the new place the user is chatting from, it is added as a platform too) as also stated, the conversations are not unified, just the other things like orders, products etc. now if its a new user, the new user creation process starts. now where is the problem I'm seeing here. there's no marker to mark each point the user is in the auth check and verication process, though it has all been defined in the model, authentication setup. there's no way to know at what point this new user or returning user is(just like current step). there has to be a way to know if this user, has been correctly checked in to this platform, or yet to verify email, etc. there has to be a way so that the bot will return the right response. an instance is lets say I'm yet to verify my email, and I ask to list available products. the bot should smartly check if this person has completed the verification process, thats inputing the code. the current implementation doesn't do that. it just assumes that this user doesn't exist yet. and with this current implementation, even if i send the code, it will likeley keep assuming that, and thus the verification might fall into a loop. I was going to propose, that a temporary conversation be saved each time a new user is registering or reauthenticating (wether from a new device or logged out and logging in again) this temp conversation will always serve as a marker and will be edited to mark the current phase of authenticating, and the platformId on the conversation is what will be used to know that it is coming from this particular platform and a specific user. the temporary conversation is like a step and after all the steps are completed, the user is now allowed to continue normal chatting, and the temp conversation is deleted. I believe, it will allow tailoring the response for each step, and alsonprevent unecessaraly calling the ai providers which will save money and token, until the user is authenticated. of cause if this is not the best approach, you are welcome to change it.

the next thing is on the flow, now I don't so much as know what happens when a flow is completed on whatsapp, or a telegram callback handler is clicked. but if I'm correct, a webhook is sent with the result(I stand to be corrected), or in the case that it should open an external link, like for payment, etc, a link is opened with the parameters passed in. now, there's no way for me to handle that esp in whatsapp, I think for telegram, the telegram_callbak_handler does that, but to an extent and it has not be hooked up yet, to listen, its just like its just there(update it if it needs updating).  but for the whatsapp, I have a flow that was created, but I dont know were to place it(that is, create a flow on whatsapp for it?), and I dont know how to process it yet, (if it needs a file create it too). I will add the flow to the prompt here. for the products, a template message is good enough, with a button for the user to buy, which opens a flow message(I don't know if flow message allows carousel of photos, if not, if the product has more than one image, the image can be attached to the flow, as the user checks the product and starts the order processing). now, a catalog of products will come in handy, when the users asks for list of products, a catalog template is sent(check the spec file for info on that). so I think this is the it, work on what I've just asked for now, in order of priority as you deem fit.

